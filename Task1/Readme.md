# Task 1

For name search *elastic search* is used

## Ensure docker is installed and running
## Run `setup.py` with appropraite permissions
## Run search.sh with name as
```
./search.sh "Katherin"
```
